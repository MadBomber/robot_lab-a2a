{"config":{"lang":["en"],"separator":"[\\s\\-,:!=\\[\\]()\"`/]+|\\.(?!\\d)|&[lg]t;|(?!\\b)(?=[A-Z][a-z])","pipeline":["stopWordFilter"],"fields":{"title":{"boost":1000.0},"text":{"boost":1.0},"tags":{"boost":1000000.0}}},"docs":[{"location":"","title":"robot_lab-a2a","text":"<p><code>robot_lab-a2a</code> is an Agent2Agent (A2A) protocol adapter for RobotLab. It wraps RobotLab robots and networks in an HTTP+SSE server so any A2A-compliant client can invoke them, receive streaming events, and participate in multi-turn conversations \u2014 all without a terminal.</p>"},{"location":"#how-it-fits-together","title":"How it fits together","text":""},{"location":"#key-concepts","title":"Key concepts","text":"<ul> <li>Server (<code>RobotLab::A2A::Server</code>) \u2014 fluent builder that registers robots and networks as A2A agents, then starts an HTTP+SSE server via <code>simple_a2a</code>.</li> <li>RobotAdapter \u2014 wraps any <code>RobotLab::Robot</code>. Supports three modes: <code>:none</code> (synchronous), <code>:a2a_tool</code> (injects <code>AskUserTool</code> for multi-turn via Thread+Queue), and <code>:io_bridge</code> (replaces robot I/O streams for robots that use <code>gets</code>/<code>puts</code> directly).</li> <li>NetworkAdapter \u2014 wraps a <code>RobotLab::Network</code>. Runs the full pipeline in <code>:none</code> mode and returns the last text content.</li> <li>AskUserTool \u2014 drop-in replacement for <code>RobotLab::AskUser</code>. Converts a blocking terminal prompt into an A2A <code>input_required</code> event and waits for a client resume.</li> <li>IoBridge \u2014 IO-compatible object that buffers robot output and converts <code>gets</code> calls into <code>input_required</code> events, enabling interactive mode for robots that talk to raw streams.</li> <li>Registry \u2014 thread-safe singleton (Mutex-protected) keyed by A2A task ID. Holds <code>Entry(thread, event_queue, answer_queue)</code> so HTTP resume requests can find and unblock the correct robot thread.</li> </ul>"},{"location":"#documentation-pages","title":"Documentation pages","text":"<ul> <li>Getting Started \u2014 installation, first server, client usage</li> <li>Interactive Modes \u2014 <code>:none</code>, <code>:a2a_tool</code>, <code>:io_bridge</code> in depth</li> <li>Server API \u2014 full constructor and method reference</li> <li>Examples \u2014 walkthrough of the bundled example scripts</li> </ul>"},{"location":"examples/","title":"Examples","text":""},{"location":"examples/#layout","title":"Layout","text":"<pre><code>examples/\n  run                     # launcher script\n  01_sync_robot/\n    server.rb             # starts the A2A server\n    client.rb             # sends a task and prints the result\n  02_interactive_a2a_tool/\n    server.rb\n    client.rb\n  03_robot_network/\n    server.rb\n    client.rb\n</code></pre>"},{"location":"examples/#how-to-run","title":"How to run","text":"<p>From the repo root, pass the example directory name to <code>examples/run</code>:</p> <pre><code>bundle exec ruby examples/run 01_sync_robot\n</code></pre> <p>From inside <code>examples/</code> directly:</p> <pre><code>cd examples\n./run 01_sync_robot\n</code></pre> <p>The launcher starts <code>server.rb</code> in the background, waits for it to bind, then runs <code>client.rb</code> in the foreground so you see the output. Press Ctrl-C to stop.</p> <p>To run server and client separately (useful for inspecting raw SSE output):</p> <pre><code># Terminal 1\nbundle exec ruby examples/01_sync_robot/server.rb\n\n# Terminal 2\nbundle exec ruby examples/01_sync_robot/client.rb\n</code></pre>"},{"location":"examples/#01_sync_robot","title":"01_sync_robot","text":"<p>Demonstrates: The simplest possible integration. A robot with no user prompts is registered in <code>:none</code> mode and invoked with a single text message.</p> <p>What it shows:</p> <ul> <li><code>Server.new.add_robot(...).run(port:)</code> pattern</li> <li>A single <code>tasks/send</code> POST</li> <li>Parsing the SSE stream to extract <code>task_complete</code> payload</li> </ul> <p>Expected output (client):</p> <pre><code>Sending task...\n[event: task_started]\n[event: task_complete]\nReply: The answer is 42.\nDone.\n</code></pre>"},{"location":"examples/#02_interactive_a2a_tool","title":"02_interactive_a2a_tool","text":"<p>Demonstrates: A robot that calls <code>RobotLab::AskUser</code> during execution. The gem injects <code>AskUserTool</code>, converts the blocking call to an A2A <code>input_required</code> event, and the client resumes with the answer.</p> <p>Two-turn flow:</p> <ol> <li>Client sends initial task \u2192 server starts robot thread \u2192 robot calls <code>AskUser(\"What is your name?\")</code> \u2192 SSE delivers <code>input_required</code> event with prompt and task ID.</li> <li>Client sends a second <code>tasks/send</code> with the task ID and the user's answer \u2192 <code>AskUserTool</code> unblocks \u2192 robot continues \u2192 SSE delivers <code>task_complete</code>.</li> </ol> <p>What it shows:</p> <ul> <li><code>interactive: :a2a_tool</code> server setup</li> <li>How to extract <code>task_id</code> and <code>input_required</code> prompt from the first SSE stream</li> <li>How to construct the resume request with <code>task_id</code></li> </ul> <p>Expected output (client):</p> <pre><code>Turn 1 \u2014 sending initial task...\n[event: task_started]\n[event: input_required] prompt: \"What is your name?\"\ntask_id: abc-123-def\n\nTurn 2 \u2014 resuming with answer...\n[event: task_started]\n[event: task_complete]\nReply: Hello, Alice! Nice to meet you.\nDone.\n</code></pre>"},{"location":"examples/#03_robot_network","title":"03_robot_network","text":"<p>Demonstrates: A <code>RobotLab::Network</code> (multi-stage pipeline) exposed as a single A2A agent via <code>NetworkAdapter</code> in <code>:none</code> mode.</p> <p>Pipeline stages: The example network typically chains two or three robots \u2014 for example, a research robot that gathers context, followed by a synthesis robot that produces a final answer. Each stage's output feeds the next.</p> <p>What it shows:</p> <ul> <li><code>add_network</code> registration</li> <li>That the caller sees only one A2A endpoint regardless of how many internal stages the network has</li> <li><code>network.run(message: text).last_text_content</code> as the final reply</li> </ul> <p>Expected output (client):</p> <pre><code>Sending task to network...\n[event: task_started]\n[event: task_complete]\nReply: [synthesised answer from the final pipeline stage]\nDone.\n</code></pre>"},{"location":"getting-started/","title":"Getting Started","text":""},{"location":"getting-started/#requirements","title":"Requirements","text":"<ul> <li>Ruby &gt;= 3.2</li> <li>The <code>robot_lab</code> gem (provides <code>RobotLab::Robot</code> and <code>RobotLab::Network</code>)</li> <li>Bundler</li> </ul>"},{"location":"getting-started/#installation","title":"Installation","text":""},{"location":"getting-started/#as-a-gem-dependency","title":"As a gem dependency","text":"<p>Add to your <code>Gemfile</code>:</p> <pre><code>gem \"robot_lab-a2a\"\n</code></pre> <p>Then run:</p> <pre><code>bundle install\n</code></pre>"},{"location":"getting-started/#as-a-local-path-dependency-for-development","title":"As a local path dependency (for development)","text":"<p>If you are working directly in this repo or have a local checkout of <code>robot_lab-a2a</code>:</p> <pre><code># Gemfile\ngem \"robot_lab-a2a\", path: \"../robot_lab-a2a\"\n</code></pre> <p>The gem also depends on <code>simple_a2a</code>. For interactive resume support (<code>input_required</code>/resume lifecycle), you need a build of <code>simple_a2a</code> that includes <code>ResumeContext</code>. If your local copy is newer than the released gem, use a path dependency for that too:</p> <pre><code>gem \"simple_a2a\", path: \"../simple_a2a\"\n</code></pre>"},{"location":"getting-started/#sync-robot-5-minutes","title":"Sync Robot (5 minutes)","text":""},{"location":"getting-started/#1-define-your-robot","title":"1. Define your robot","text":"<pre><code># my_robot.rb\nrequire \"robot_lab\"\n\nclass GreeterRobot &lt; RobotLab::Robot\n  def initialize\n    super(\n      name: \"Greeter\",\n      description: \"Returns a greeting for any input\",\n      model: \"gpt-4o-mini\"\n    )\n  end\n\n  # robot_lab calls run(text) and expects a result with a .reply method\nend\n</code></pre>"},{"location":"getting-started/#2-start-the-server","title":"2. Start the server","text":"<pre><code># server.rb\nrequire \"robot_lab/a2a\"\nrequire_relative \"my_robot\"\n\nrobot = GreeterRobot.new\n\nRobotLab::A2A::Server.new\n  .add_robot(robot, name: \"Greeter\", description: \"Returns a greeting\")\n  .run(port: 7000)\n</code></pre> <pre><code>bundle exec ruby server.rb\n# =&gt; Listening on http://0.0.0.0:7000\n</code></pre>"},{"location":"getting-started/#3-send-a-task","title":"3. Send a task","text":"<p>The server exposes each robot at its DNS-label path. <code>GreeterRobot</code> registered as <code>\"Greeter\"</code> becomes <code>/greeter</code>.</p> <pre><code>curl -X POST http://localhost:7000/greeter/tasks/send \\\n  -H \"Content-Type: application/json\" \\\n  -d '{\"message\": {\"role\": \"user\", \"parts\": [{\"text\": \"Hello!\"}]}}'\n</code></pre> <p>The response is a stream of SSE events ending with a <code>task_complete</code> event whose payload contains the robot's reply.</p>"},{"location":"getting-started/#4-inspect-the-agent-card","title":"4. Inspect the agent card","text":"<p>Every registered agent exposes its capabilities at <code>/.well-known/agent.json</code> (via <code>simple_a2a</code>):</p> <pre><code>curl http://localhost:7000/greeter/.well-known/agent.json\n</code></pre>"},{"location":"getting-started/#whats-next","title":"What's next","text":"<ul> <li>Interactive Modes \u2014 enable multi-turn conversations with <code>:a2a_tool</code> or <code>:io_bridge</code></li> <li>Server API \u2014 full reference for <code>Server.new</code>, <code>add_robot</code>, <code>add_network</code>, <code>run</code>, and <code>to_app</code></li> </ul>"},{"location":"interactive-modes/","title":"Interactive Modes","text":""},{"location":"interactive-modes/#why-interactive-matters","title":"Why interactive matters","text":"<p>RobotLab's <code>AskUser</code> tool blocks the robot thread and reads from a terminal. That works fine for CLI scripts but breaks completely in an HTTP server: there is no terminal, and the HTTP request has already returned by the time the robot needs an answer.</p> <p><code>robot_lab-a2a</code> solves this by converting blocking terminal prompts into A2A <code>input_required</code> events. The robot thread pauses on a Ruby <code>Queue</code>. The HTTP layer serialises the prompt as an SSE event to the A2A client. When the client sends a resume request, the answer is placed onto the queue and the robot thread continues \u2014 all without any terminal.</p> <p>Three modes are available. Choose based on how your robot is implemented.</p>"},{"location":"interactive-modes/#none-mode","title":"<code>:none</code> mode","text":"<p>The default. The robot receives the initial text message, runs to completion, and returns a single reply. No user interaction occurs during execution.</p> <pre><code>RobotLab::A2A::Server.new                        # interactive: :none is the default\n  .add_robot(robot, name: \"Summariser\", description: \"Summarises text\")\n  .run(port: 7000)\n</code></pre> <p>Robot interface required: <code>robot.run(text)</code> returns a result responding to <code>.reply</code>.</p> <p>Use <code>:none</code> when your robot never calls <code>AskUser</code> or reads from stdin.</p>"},{"location":"interactive-modes/#a2a_tool-mode","title":"<code>:a2a_tool</code> mode","text":"<p>The server injects an <code>AskUserTool</code> instance into <code>robot.local_tools</code> before the robot thread starts, and removes it on teardown. <code>AskUserTool</code> is a drop-in replacement for <code>RobotLab::AskUser</code> that uses <code>Queue</code> instead of <code>$stdin</code>.</p> <p>Turn 1 \u2014 initial request:</p> <pre><code>curl -X POST http://localhost:7000/my-robot/tasks/send \\\n  -H \"Content-Type: application/json\" \\\n  -d '{\"message\": {\"role\": \"user\", \"parts\": [{\"text\": \"Plan a trip\"}]}}'\n</code></pre> <p>The server starts the robot on a new Thread, keyed in <code>Registry</code> by the A2A task ID. When the robot calls <code>AskUser</code>, <code>AskUserTool#execute</code>:</p> <ol> <li>Pushes <code>{type: :ask, prompt: \"Where do you want to go?\"}</code> onto <code>event_queue</code>.</li> <li>Blocks on <code>answer_queue</code>.</li> </ol> <p>The SSE stream delivers an <code>input_required</code> event to the client containing the prompt text and the task ID.</p> <p>Turn 2 \u2014 resume:</p> <pre><code>curl -X POST http://localhost:7000/my-robot/tasks/send \\\n  -H \"Content-Type: application/json\" \\\n  -d '{\n    \"task_id\": \"&lt;task-id-from-turn-1&gt;\",\n    \"message\": {\"role\": \"user\", \"parts\": [{\"text\": \"Paris\"}]}\n  }'\n</code></pre> <p><code>simple_a2a</code> routes this to the resume handler. The server looks up the task in <code>Registry</code>, places <code>\"Paris\"</code> onto <code>answer_queue</code>, and the blocked <code>AskUserTool#execute</code> returns <code>\"Paris\"</code> to the robot. The robot continues. If it calls <code>AskUser</code> again, the cycle repeats. When the robot finishes, a <code>task_complete</code> event closes the SSE stream.</p> <p>Setup:</p> <pre><code>RobotLab::A2A::Server.new(interactive: :a2a_tool)\n  .add_robot(robot, name: \"Planner\", description: \"Plans trips interactively\")\n  .run(port: 7000)\n</code></pre> <p>Robot interface required:</p> <ul> <li><code>robot.run(text)</code> \u2014 entry point.</li> <li><code>robot.local_tools</code> \u2014 mutable <code>Array</code> that the server prepends <code>AskUserTool</code> to. The instance variable is <code>@local_tools</code>.</li> </ul> <p>Important: Interactive resume requires a build of <code>simple_a2a</code> that includes <code>ResumeContext</code> support. See Getting Started \u2014 Installation for the path dependency pattern.</p>"},{"location":"interactive-modes/#io_bridge-mode","title":"<code>:io_bridge</code> mode","text":"<p>For robots that communicate via raw Ruby IO (<code>puts</code>/<code>gets</code>) rather than the <code>AskUser</code> tool. The server replaces <code>robot.input</code> and <code>robot.output</code> with an <code>IoBridge</code> instance before starting the robot thread, and restores the originals on teardown.</p> <p><code>IoBridge</code> behaviour:</p> <ul> <li>Writes (<code>write</code>, <code>puts</code>, <code>print</code>) \u2014 accumulate into an internal buffer.</li> <li><code>gets</code> \u2014 flushes the buffer as an <code>input_required</code> SSE event (the buffered text becomes the prompt), then blocks on <code>answer_queue</code> until the A2A client resumes. The answer is returned from <code>gets</code> to the robot.</li> </ul> <p>The two-turn client pattern is identical to <code>:a2a_tool</code>.</p> <pre><code>RobotLab::A2A::Server.new(interactive: :io_bridge)\n  .add_robot(robot, name: \"IO Robot\", description: \"Uses gets/puts for interaction\")\n  .run(port: 7000)\n</code></pre> <p>Robot interface required:</p> <ul> <li><code>robot.input=</code> / <code>robot.output=</code> \u2014 settable IO attributes.</li> <li>The robot must use <code>@input.gets</code> and <code>@output.puts</code> (or equivalent) rather than <code>$stdin</code>/<code>$stdout</code> directly.</li> </ul> <p>Important: Interactive resume requires <code>simple_a2a</code> with <code>ResumeContext</code> support.</p>"},{"location":"interactive-modes/#choosing-a-mode","title":"Choosing a mode","text":"Mode Robot interface required Typical use case <code>:none</code> <code>robot.run(text) \u2192 result.reply</code> Fully autonomous robots with no user prompts <code>:a2a_tool</code> <code>robot.run(text)</code> + mutable <code>robot.local_tools</code> Robots that use <code>RobotLab::AskUser</code> as a tool <code>:io_bridge</code> <code>robot.run(text)</code> + settable <code>robot.input=</code> / <code>robot.output=</code> Robots that interact via raw stdin/stdout streams <p>When in doubt, start with <code>:none</code>. Upgrade to <code>:a2a_tool</code> if your robot uses <code>AskUser</code>, or <code>:io_bridge</code> if it uses raw IO.</p>"},{"location":"server-api/","title":"Server API Reference","text":"<p><code>RobotLab::A2A::Server</code> is a fluent builder. Every mutating method returns <code>self</code> so calls can be chained.</p>"},{"location":"server-api/#constructor","title":"Constructor","text":"<pre><code>RobotLab::A2A::Server.new(\n  host:        \"localhost\",  # String  \u2014 bind address\n  port:        9292,         # Integer \u2014 TCP port\n  storage:     nil,          # Object  \u2014 task storage backend (simple_a2a compatible); nil uses in-memory default\n  interactive: :none         # Symbol  \u2014 default interactive mode for all registered adapters\n)\n</code></pre> Parameter Type Default Notes <code>host</code> <code>String</code> <code>\"localhost\"</code> Passed through to <code>simple_a2a</code> / Falcon <code>port</code> <code>Integer</code> <code>9292</code> TCP port to listen on <code>storage</code> Object / nil <code>nil</code> Pluggable task persistence; defaults to an in-memory store <code>interactive</code> <code>Symbol</code> <code>:none</code> Applies to all adapters unless overridden per-registration <p>Valid <code>interactive</code> values: <code>:none</code>, <code>:a2a_tool</code>, <code>:io_bridge</code>. See Interactive Modes for details.</p>"},{"location":"server-api/#add_robot","title":"<code>add_robot</code>","text":"<pre><code>server.add_robot(\n  robot,\n  name:        required,   # String \u2014 human-readable agent name\n  description: required,   # String \u2014 agent capability description\n  path:        nil         # String \u2014 URL path segment; derived from name if nil\n) \u2192 self\n</code></pre> <p>Wraps <code>robot</code> in a <code>RobotAdapter</code> using the server's <code>interactive</code> setting and registers it with <code>simple_a2a</code>.</p> <p><code>robot</code> must respond to:</p> <ul> <li><code>robot.name</code> \u2192 <code>String</code></li> <li><code>robot.description</code> \u2192 <code>String</code></li> <li><code>robot.run(text)</code> \u2192 object responding to <code>.reply</code></li> <li>For <code>:a2a_tool</code>: <code>robot.local_tools</code> \u2192 mutable <code>Array</code> (backed by <code>@local_tools</code>)</li> <li>For <code>:io_bridge</code>: <code>robot.input=</code> and <code>robot.output=</code></li> </ul> <p>Path defaulting: If <code>path:</code> is omitted, it is derived from <code>name</code> by applying the DNS label conversion rule (see below). Example: <code>name: \"My Robot\"</code> \u2192 path <code>/my-robot</code>.</p>"},{"location":"server-api/#add_network","title":"<code>add_network</code>","text":"<pre><code>server.add_network(\n  network,\n  name:        required,   # String \u2014 human-readable agent name\n  description: required,   # String \u2014 agent capability description\n  path:        nil         # String \u2014 URL path segment; derived from name if nil\n) \u2192 self\n</code></pre> <p>Wraps <code>network</code> in a <code>NetworkAdapter</code>. Only <code>:none</code> mode is supported for networks; the <code>interactive</code> setting is ignored.</p> <p><code>network</code> must respond to:</p> <ul> <li><code>network.run(message: text)</code> \u2192 object responding to <code>.last_text_content</code></li> </ul> <p>Path defaulting: Same DNS label conversion as <code>add_robot</code>.</p>"},{"location":"server-api/#add_robot-optional-keyword-details","title":"<code>add_robot</code> \u2014 optional keyword details","text":"<p><code>name:</code> and <code>description:</code> default to <code>robot.name</code> and <code>robot.description</code> when omitted. <code>path:</code> always defaults to the DNS label of the name used.</p>"},{"location":"server-api/#run","title":"<code>run</code>","text":"<pre><code>server.run\n</code></pre> <p>Starts the Falcon HTTP server (blocking). Host and port are set in the constructor. Does not return until the server is stopped.</p>"},{"location":"server-api/#to_app","title":"<code>to_app</code>","text":"<pre><code>app = server.to_app  # \u2192 Rack::URLMap\n</code></pre> <p>Returns a <code>Rack::URLMap</code> instead of starting a server. Use this to embed <code>robot_lab-a2a</code> inside an existing Rack application such as Rails or Puma.</p> <p>Example <code>config.ru</code>:</p> <pre><code>require \"robot_lab/a2a\"\nrequire_relative \"my_robot\"\n\nrobot = MyRobot.new\na2a = RobotLab::A2A::Server.new\n        .add_robot(robot, name: \"Assistant\", description: \"General assistant\")\n\nrun Rack::URLMap.new(\n  \"/\"     =&gt; MyRailsApp,\n  \"/a2a\"  =&gt; a2a.to_app\n)\n</code></pre>"},{"location":"server-api/#dns-label-conversion-rule","title":"DNS label conversion rule","text":"<p>Agent names are converted to RFC 1123-compatible path segments:</p> <ol> <li>Convert to lowercase.</li> <li>Replace underscores with hyphens.</li> <li>Drop any character that is not an ASCII letter, digit, or hyphen.</li> <li>Strip leading/trailing hyphens.</li> </ol> <p>Examples:</p> Name Path <code>\"My Robot\"</code> <code>/my-robot</code> <code>\"order_processor\"</code> <code>/order-processor</code> <code>\"GPT-4 Agent!\"</code> <code>/gpt-4-agent</code>"}]}